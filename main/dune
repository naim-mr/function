(executable
 (public_name function)
 (name main)
 (promote
  (until-clean)
  (into ..))
 (flags
  (:standard -open Intraproc -open Frontend -open Utils))
 (libraries yojson  intraproc frontend utils domains))

(env
 (dev
  (flags
   (:standard -w -9-27-32-33-35 -warn-error -A))))
