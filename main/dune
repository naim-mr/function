(executable
 (public_name function)
 (name main)
 (promote
  (until-clean)
  (into ..))
 (flags
  (:standard -open Frontend -open Domains -open Utils))
 (libraries yojson frontend utils domains str))

(env
 (dev
  (flags
   (:standard -w -9-27-32-33-35 -warn-error -A))))
