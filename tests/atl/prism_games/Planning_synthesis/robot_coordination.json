{
    "analysis": "atl",
    "property": "<r1,r2>F{AND{x1 >= goal}{x2 >= goal}}",
    "domain": "polyhedra",
    "description": "Robot coordination (de-probabilised). The coalition of both robots brings BOTH to their goal cells despite the adversarial slips -> expected TRUE. Probabilistic move-failure is abstracted as actuator-dominance: a commanded step is +2 and a slip is -1, so a pushing robot nets +1 per round whatever the (unbounded) slips. The goal cell is arbitrary (input(\"env\"), not fixed in advance).",
    "model": "robot_coordination.c"
}
