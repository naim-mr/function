{
    "analysis": "atl",
    "property": "<ctrl>U{operational == 1}{t >= deadline}",
    "domain": "polyhedra",
    "description": "Embedded control system -- mission survival (until). The controller (coalition) keeps the system operational at every step UNTIL the mission deadline is reached -> expected TRUE. Discrete, qualitative analog of the PRISM time-bounded property P=?[ true U<=T down ] (here: stay up for the whole horizon). The deadline is arbitrary, not fixed in advance.",
    "model": "embedded_control.c"
}
