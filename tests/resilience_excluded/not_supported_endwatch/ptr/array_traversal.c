int main(){
    int x = ?; //{} & *(&x) = top 
    int* ptr = &x;
    // *(&x) < 0 => 3 & *(&x) >=0 => *(&x) 
    // while(*ptr >= 0){
    //     //{ ptr -> &x} & *(&x) >= 0 
    //     // *(&x) < 1 => 5 
    //     x = x - 1;
    //     // *(&x) < 0 => 4
    // }
    // //{ ptr -> &x} & *(&x) == 0 
    // _ => 0
    return 0;
}